name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup pnpm cache
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
          cache: pnpm

      - name: Enable Corepack + pnpm
        run: |
          corepack enable
          pnpm -v

      - name: Show runtime versions
        run: node -v && pnpm -v

      - name: Install dependencies (deterministic)
        run: pnpm install --frozen-lockfile

      - name: Validate Cloudflare secrets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || (echo "CF_API_TOKEN is empty" && exit 1)
          test -n "$CLOUDFLARE_ACCOUNT_ID" || (echo "CF_ACCOUNT_ID is empty" && exit 1)

      - name: Validate migration filename convention
        run: |
          set -e
          for f in d1/migrations/*.sql; do
            b=$(basename "$f")
            echo "checking $b"
            echo "$b" | grep -Eq '^[0-9]{12}__[a-z0-9_]+\.sql$' || {
              echo "invalid migration filename: $b"
              echo "expected: YYYYMMDDHHmm__desc.sql"
              exit 1
            }
          done

      - name: Ensure schema_migrations exists (prod)
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: npx wrangler d1 execute 1acc05d4-49bb-4306-8115-5646a945dc9c --remote --file=./d1/migrations/202602230835__create_schema_migrations.sql

      - name: Apply migration if needed (prod)
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npx wrangler d1 execute 1acc05d4-49bb-4306-8115-5646a945dc9c --remote --command "PRAGMA table_info(users);" > /tmp/users_columns.txt
          if grep -q "disabled" /tmp/users_columns.txt; then
            echo "prod migration not needed"
          else
            echo "prod migration applying"
            npx wrangler d1 execute 1acc05d4-49bb-4306-8115-5646a945dc9c --remote --file=./d1/migrations/202602221000__add_user_disabled.sql
            npx wrangler d1 execute 1acc05d4-49bb-4306-8115-5646a945dc9c --remote --command "INSERT OR IGNORE INTO schema_migrations (migration_name) VALUES ('202602221000__add_user_disabled.sql');"
          fi

      - name: Ensure schema_migrations exists (dev)
        if: github.ref == 'refs/heads/dev'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: npx wrangler d1 execute b188ca51-b44e-48c5-8463-baf3091fc279 --remote --file=./d1/migrations/202602230835__create_schema_migrations.sql

      - name: Apply migration if needed (dev)
        if: github.ref == 'refs/heads/dev'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npx wrangler d1 execute b188ca51-b44e-48c5-8463-baf3091fc279 --remote --command "PRAGMA table_info(users);" > /tmp/users_columns_dev.txt
          if grep -q "disabled" /tmp/users_columns_dev.txt; then
            echo "dev migration not needed"
          else
            echo "dev migration applying"
            npx wrangler d1 execute b188ca51-b44e-48c5-8463-baf3091fc279 --remote --file=./d1/migrations/202602221000__add_user_disabled.sql
            npx wrangler d1 execute b188ca51-b44e-48c5-8463-baf3091fc279 --remote --command "INSERT OR IGNORE INTO schema_migrations (migration_name) VALUES ('202602221000__add_user_disabled.sql');"
          fi

      - name: Migration guard (production)
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npx wrangler d1 execute 1acc05d4-49bb-4306-8115-5646a945dc9c --remote --command "PRAGMA table_info(users);" > /tmp/users_columns_prod_guard.txt
          grep -q "disabled" /tmp/users_columns_prod_guard.txt || (echo "migration guard failed: users.disabled missing" && exit 1)

      - name: Deploy main (production)
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: npx -y @opennextjs/cloudflare@1.16.5 build && npx -y @opennextjs/cloudflare@1.16.5 deploy

      - name: Smoke test (production)
        if: github.ref == 'refs/heads/main'
        run: |
          set -e
          curl -fsS https://sinon.live/ >/dev/null
          curl -fsS https://sinon.live/login >/dev/null
          curl -fsSI https://sinon.live/api/auth/signin/google | head -n 1 | grep -qE "HTTP/[0-9.]+ (200|302)"

      - name: Migration guard (dev)
        if: github.ref == 'refs/heads/dev'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npx wrangler d1 execute b188ca51-b44e-48c5-8463-baf3091fc279 --remote --command "PRAGMA table_info(users);" > /tmp/users_columns_dev_guard.txt
          grep -q "disabled" /tmp/users_columns_dev_guard.txt || (echo "migration guard failed: users.disabled missing" && exit 1)

      - name: Deploy dev (staging)
        if: github.ref == 'refs/heads/dev'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: npx -y @opennextjs/cloudflare@1.16.5 build && npx -y @opennextjs/cloudflare@1.16.5 deploy --env dev

      - name: Smoke test (dev)
        if: github.ref == 'refs/heads/dev'
        run: |
          set -e
          curl -fsS https://dev.sinon.live/ >/dev/null
          curl -fsS https://dev.sinon.live/login >/dev/null
          curl -fsSI https://dev.sinon.live/api/auth/signin/google | head -n 1 | grep -qE "HTTP/[0-9.]+ (200|302)"
